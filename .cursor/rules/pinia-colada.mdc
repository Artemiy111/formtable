---
description: "Pinia Colada"
alwaysApply: false
---

you MUST use @pinia/colada (analogue of tanstack query) for queries and mutations

you MUST define coupled queries/mutations together in one file src/shared/model/smth.ts

you MUST export model from src/model/index.tsyou MUST use @pinia/colada (analogue of tanstack query) for queries and mutations

you MUST define coupled queries/mutations together in one file src/shared/model/smth.ts

you MUST export model from src/model/index.ts